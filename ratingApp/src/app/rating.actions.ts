import { createAction } from '@ngrx/store';


export const increment = createAction('[Rating Component] Increment');
export const decrement = createAction('[Rating Component] Decrement');
export const reset = createAction('[Raiting Component] Reset');